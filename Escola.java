package Membros;
import java.util.Scanner;
import java.util.Date;
public class Escola {

	public static void main(String[] args) {
		new Alunos ("nome", "cpf", new Date());
		new Alunos ("nome", "cpf", new Date());
		new Alunos ("nome", "cpf", new Date());
		new Alunos ("nome", "cpf", new Date());
	}
}